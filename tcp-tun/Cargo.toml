[package]
name = "tcp-tun"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Linux TUN backend + live demo for the userspace TCP stack"

[dependencies]
tcp-core = { path = "../tcp-core" }

[[bin]]
name = "tcp-tun"
path = "src/main.rs"
