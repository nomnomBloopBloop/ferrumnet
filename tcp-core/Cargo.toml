[package]
name = "tcp-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Device-agnostic, sans-IO TCP/IP engine (zero dependencies, std only)"

[dependencies]
# Intentionally empty: this crate depends only on the Rust standard library.
